<div class="apod-card">
    <div class="apod-image">
        {{#if apod.is_image }}
            <img class="apod-img" src="{{ apod.url }}">
        {{else}}
            <p>Today's picture is a video or another media type. <a href="{{ apod.url }}">Please click here to view</a></p>
        {{/if}}
    </div>
    <div class="apod-body">
        <div class="apod-head">
            <h1>{{apod.title}}</h1>
        </div>
        <div class="apod-text">
            <p>{{ apod.credit }}</p>
            <p>{{ apod.explanation }}</p>
            <p>This is NASA's Astronomy Picture of the Day for {{ apod.date }}.</p>
            <p><a href="{{ apod.nasa_url }}">See this image at NASA's website</a></p>
            <p>This is made by asouer for WebDev Class</p>
        </div>
        <div class="apod-foot">
            <a class="left" href="{{ apod.nasa_url }}">See this image at NASA's website</a>
            <div class="add-favorites-form right">
                <form action="favorites/add" method="POST">
                    <input type="hidden" name="date" value="{{apod.date}}">
                    <input type="hidden" name="title" value="{{apod.title}}">
                    <input type="hidden" name="url" value="{{apod.url}}">
                    <input type="hidden" name="nasa_url" value="{{apod.nasa_url}}">
                    <input type="submit" name="add" value="Add to favorites">
                </form>
            </div>
        </div>
    </div>
</div>